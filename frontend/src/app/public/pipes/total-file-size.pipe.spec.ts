import { TotalFileSizePipe } from './total-file-size.pipe';

describe('TotalFileSizePipe', () => {
  it('create an instance', () => {
    const pipe = new TotalFileSizePipe();
    expect(pipe).toBeTruthy();
  });
});
